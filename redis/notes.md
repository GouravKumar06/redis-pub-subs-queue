**Redis**
``` in-memory data structure store used as db,cache,message broker and streaming engine ```


``` in memory ---->  means data store in RAM ```


**LPush**     ```lPush message 2```
**LPOP**      ```LPOP message ```
**RPOP**      ```RPOP message ```
**BLPOP**     ```BLPOP message 20```  ```time can give```


**Sadd**           ``used for add set``  ``sadd ip 2``
**srem**           ``used for remove``  ``srem ip 2``